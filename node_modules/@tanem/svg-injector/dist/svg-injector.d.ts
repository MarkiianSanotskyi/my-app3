import { DoneCallback, Errback, EvalScripts } from './types';
declare type Elements = HTMLCollectionOf<Element> | NodeListOf<Element> | Element | null;
interface OptionalArgs {
    done?: DoneCallback;
    each?: Errback;
    evalScripts?: EvalScripts;
    renumerateIRIElements?: boolean;
}
declare const SVGInjector: (elements: Elements, { done, each, evalScripts, renumerateIRIElements }?: OptionalArgs) => void;
export default SVGInjector;
